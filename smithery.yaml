# Smithery.ai configuration for HTTP deployment
# Uses TypeScript runtime with Smithery CLI for automatic containerization

runtime: "typescript"

# Optional environment variables for production
env:
  NODE_ENV: "production"
